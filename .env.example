# The public origin of the deployment — used for canonical URLs, the sitemap
# and robots.txt. Leave unset for local development (defaults to
# http://localhost:3000). No other configuration is needed: the app has no
# backend and no API keys.
NEXT_PUBLIC_SITE_URL=https://example.com
